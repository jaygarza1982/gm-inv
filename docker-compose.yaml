version: '3'

services:
    api-server:
        container_name: "gm-inv-api-server"
        build:
            context: .
        volumes:
            - ./:/app
            - /app/node_modules
        ports:
            - 3001:3000
            - 9229:9229
        environment: 
            - DATA_FOLDER=/app/csv-files